name: myapp
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Dependências SQLite compatíveis com seu SDK
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  sqflite_common_ffi: ^2.3.4+4 # Última versão compatível com seu SDK

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Ajustado para estabilidade

flutter:
  uses-material-design: true
